import { AuthorWorkspace } from "@/components/author/AuthorWorkspace";
import "./author.css";

export const metadata = {
  title: "Authoring Console · PolicyDebate101",
};

export default function AuthorPage() {
  return <AuthorWorkspace />;
}
